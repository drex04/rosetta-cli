---
phase: 20
plan: 5
title: "Command renames: accredit→ledger, run→transform, shacl-gen→shapes"
status: ready
decisions: [D-20-22, D-20-23]
must_haves:
  truths:
    - "`rosetta ledger append|review|dump` works end-to-end; `rosetta accredit` is not a recognized command"
    - "`rosetta transform` materializes YARRRML + data → JSON-LD; `rosetta run` is not a recognized command"
    - "`rosetta shapes` generates SHACL from LinkML; `rosetta shacl-gen` is not a recognized command"
    - "All 9 mandatory quality checks pass (ruff format, ruff check, basedpyright, pytest, radon, vulture, bandit, refurb, mkdocs build --strict)"
    - "Docs site builds cleanly with updated nav, cross-references, and command names"
  artifacts:
    - path: rosetta/cli/ledger.py
      provides: "ledger CLI command (renamed from accredit.py)"
      contains: "def cli"
    - path: rosetta/core/ledger.py
      provides: "ledger core logic (renamed from accredit.py)"
      contains: "class Ledger"
    - path: rosetta/cli/transform.py
      provides: "transform CLI command (renamed from run.py)"
      contains: "def cli"
    - path: rosetta/cli/shapes.py
      provides: "shapes CLI command (renamed from shacl_gen.py)"
      contains: "def cli"
    - path: docs/cli/ledger.md
      provides: "ledger CLI docs (renamed from accredit.md)"
      contains: "rosetta ledger"
    - path: docs/cli/transform.md
      provides: "transform CLI docs (renamed from run.md)"
      contains: "rosetta transform"
    - path: docs/cli/shapes.md
      provides: "shapes CLI docs (renamed from shacl-gen.md)"
      contains: "rosetta shapes"
  key_links:
    - from: rosetta/cli/__init__.py
      to: rosetta/cli/ledger.py
      via: "_LAZY_SUBCOMMANDS['ledger'] = 'rosetta.cli.ledger'"
    - from: rosetta/cli/__init__.py
      to: rosetta/cli/transform.py
      via: "_LAZY_SUBCOMMANDS['transform'] = 'rosetta.cli.transform'"
    - from: rosetta/cli/__init__.py
      to: rosetta/cli/shapes.py
      via: "_LAZY_SUBCOMMANDS['shapes'] = 'rosetta.cli.shapes'"
---

# Plan 20-05: Command Renames

Three mechanical renames. No behavioral changes. No backward-compat aliases.

| Old name     | New name      | CLI module           | Core module          | Doc file             |
|-------------|---------------|---------------------|---------------------|---------------------|
| `accredit`  | `ledger`      | `cli/accredit.py`→`cli/ledger.py` | `core/accredit.py`→`core/ledger.py` | `accredit.md`→`ledger.md` |
| `run`       | `transform`   | `cli/run.py`→`cli/transform.py`   | —                   | `run.md`→`transform.md`   |
| `shacl-gen` | `shapes`      | `cli/shacl_gen.py`→`cli/shapes.py`| —                   | `shacl-gen.md`→`shapes.md` |

---

## Task 1: Rename files and update registration

**Do:**

1. `git mv` the module files:
   - `rosetta/cli/accredit.py` → `rosetta/cli/ledger.py`
   - `rosetta/core/accredit.py` → `rosetta/core/ledger.py`
   - `rosetta/cli/run.py` → `rosetta/cli/transform.py`
   - `rosetta/cli/shacl_gen.py` → `rosetta/cli/shapes.py`

2. `git mv` the test files:
   - `rosetta/tests/test_accredit.py` → `rosetta/tests/test_ledger.py`
   - `rosetta/tests/test_accredit_integration.py` → `rosetta/tests/test_ledger_integration.py`
   - `rosetta/tests/test_run.py` → `rosetta/tests/test_transform.py`
   - `rosetta/tests/test_shacl_gen.py` → `rosetta/tests/test_shapes.py`

3. `git mv` the doc files:
   - `docs/cli/accredit.md` → `docs/cli/ledger.md`
   - `docs/cli/run.md` → `docs/cli/transform.md`
   - `docs/cli/shacl-gen.md` → `docs/cli/shapes.md`

4. Update `_LAZY_SUBCOMMANDS` in `rosetta/cli/__init__.py`:
   - `"accredit": "rosetta.cli.accredit"` → `"ledger": "rosetta.cli.ledger"`
   - `"run": "rosetta.cli.run"` → `"transform": "rosetta.cli.transform"`
   - `"shacl-gen": "rosetta.cli.shacl_gen"` → `"shapes": "rosetta.cli.shapes"`

**Done:** All files renamed, registration updated. Tests will fail (imports broken) — that's expected.

---

## Task 2: Update all Python imports and string references

**Do:**

For each renamed module, update every import statement and string reference across the codebase. Use `grep -r` to find all occurrences, then fix systematically.

### accredit → ledger

Update imports in these files (found via blast-radius scan):
- `rosetta/cli/ledger.py` — internal imports from `rosetta.core.accredit` → `rosetta.core.ledger`
- `rosetta/tests/test_ledger.py` — imports from `rosetta.cli.accredit` → `rosetta.cli.ledger`, `rosetta.core.accredit` → `rosetta.core.ledger`
- `rosetta/tests/test_ledger_integration.py` — same
- `rosetta/tests/test_compile.py` — imports `accredit` CLI/core
- `rosetta/tests/test_yarrrml_compile_integration.py` — imports `accredit` CLI
- `rosetta/tests/adversarial/test_schema_mismatch.py` — imports `accredit` CLI
- `rosetta/tests/adversarial/test_sssom_mistakes.py` — imports `accredit` CLI/core
- `rosetta/tests/test_suggest.py` — imports `accredit` core
- `rosetta/core/similarity.py` — may import from `accredit`
- `rosetta.toml` — `[accredit]` section → `[ledger]`
- `rosetta/core/config.py` — config key reference if any

Also update string literals: help text, error messages, docstrings that say "accredit".

### run → transform

- `rosetta/tests/test_transform.py` — imports from `rosetta.cli.run` → `rosetta.cli.transform`
- `rosetta/tests/test_yarrrml_run_e2e.py` — import
- `rosetta/tests/adversarial/test_cli_misuse.py` — import
- `rosetta/tests/adversarial/test_yarrrml_hygiene.py` — import
- `rosetta/tests/adversarial/test_yarrrml_validate_misuse.py` — import
- `rosetta/tests/integration/test_full_chain.py` — import
- `rosetta/tests/integration/test_cross_tool_pipeline.py` — import
- `rosetta/tests/integration/test_yarrrml_validate_pipeline.py` — import
- `rosetta/cli/compile.py` — cross-reference in help text ("rosetta run" → "rosetta transform")
- `rosetta/core/shapes_loader.py` — docstring reference

Also update string literals: epilog examples, error messages referencing "rosetta run".

### shacl-gen → shapes

- `rosetta/tests/test_shapes.py` — imports from `rosetta.cli.shacl_gen` → `rosetta.cli.shapes`
- `rosetta/tests/test_shacl_overrides.py` — import
- `rosetta/tests/test_cli_init.py` — "shacl-gen" string in subcommand list
- `rosetta/tests/smoke/test_entry_points.py` — "shacl-gen" string
- `rosetta/tests/fixtures/generate_fixtures.py` — import
- `rosetta/tests/integration/test_shacl_gen_chain.py` — import (also rename file → `test_shapes_chain.py`)
- `rosetta/tests/integration/test_cross_tool_pipeline.py` — import
- `rosetta/tests/integration/test_yarrrml_validate_pipeline.py` — import (just added in today's fix)

Also update string literals: epilog examples, docstrings referencing "shacl-gen" or "rosetta shacl-gen".

**Done:** `uv run pytest` passes. `uv run ruff check .` clean. `uv run basedpyright` clean.

---

## Task 3: Update documentation

**Do:**

1. **`docs/cli/ledger.md`** — update mkdocs-click directive module path, all prose references
2. **`docs/cli/transform.md`** — same
3. **`docs/cli/shapes.md`** — same
4. **`docs/cli/index.md`** — update command table (names + links)
5. **`docs/cli/compile.md`** — cross-references to "run" → "transform"
6. **`docs/cli/validate.md`** — cross-references to "shacl-gen" → "shapes"
7. **`docs/concepts/accreditation.md`** — "accredit" → "ledger"
8. **`docs/concepts/pipeline.md`** — all three renames in pipeline diagram + links
9. **`docs/getting-started.md`** — all three renames
10. **`mkdocs.yml`** — update nav entries (3 items)
11. **`README.md`** — update command table (3 rows)
12. **`CLAUDE.md`** — update subcommand list
13. **`rosetta.toml`** — `[accredit]` section → `[ledger]` if present
14. **`pyproject.toml`** — no change needed (single `rosetta` entry point)

**Done:** `uv run mkdocs build --strict` passes. README and CLAUDE.md reflect new names.

---

## Task 4: Final verification

**Do:**

Run all 9 mandatory checks:

```bash
uv run ruff format .
uv run ruff check .
uv run basedpyright
uv run pytest
uv run radon cc rosetta/core/ -n C -s
uv run vulture rosetta/ --exclude rosetta/tests --min-confidence 80
uv run bandit -r rosetta/ -x rosetta/tests -ll
uv run refurb rosetta/
uv run mkdocs build --strict
```

Verify old command names are gone:
```bash
rosetta accredit --help  # should error
rosetta run --help       # should error
rosetta shacl-gen --help # should error
```

Verify new names work:
```bash
rosetta ledger --help
rosetta transform --help
rosetta shapes --help
```

**Done:** All 9 checks green. Old names rejected. New names functional.
