# Phase 7 Plan 01 — rosetta-provenance: Stamp PROV-O Metadata

```yaml
phase: 7
plan: 01
name: rosetta-provenance
requirements: [REQ-18, REQ-19]
complexity: medium
spec: ~
must_haves:
  truths:
    - "rosetta-provenance stamp <artifact.ttl> writes PROV-O triples (Activity, Entity, Agent) into the output graph and increments rose:version by 1 on each call."
    - "rosetta-provenance query <artifact.ttl> prints a human-readable summary of all stamp events (agent, timestamp, version, label); --format json emits a JSON array."
    - "Stamped artifacts remain valid RDF Turtle parseable by rdflib with no errors."
    - "Running stamp twice on the same artifact produces rose:version 1 then rose:version 2 with two distinct prov:Activity nodes."
    - "All new functions in rosetta/core/provenance.py and rosetta/cli/provenance.py carry full type annotations; basedpyright passes with zero errors."
  artifacts:
    - path: rosetta/core/provenance.py
      provides: PROV-O triple generation library
      contains: "def stamp_artifact"
    - path: rosetta/core/models.py
      provides: ProvenanceRecord Pydantic model
      contains: "class ProvenanceRecord"
    - path: rosetta/cli/provenance.py
      provides: rosetta-provenance CLI (stamp + query subcommands)
      contains: "@cli.command()"
    - path: rosetta/tests/test_provenance.py
      provides: pytest tests for stamp and query
      contains: "def test_stamp_increments_version"
  key_links:
    - from: rosetta/cli/provenance.py
      to: rosetta/core/provenance.py
      via: "from rosetta.core.provenance import stamp_artifact, query_provenance"
    - from: rosetta/cli/provenance.py
      to: rosetta/core/models.py
      via: "from rosetta.core.models import ProvenanceRecord"
    - from: rosetta/core/provenance.py
      to: rosetta/core/rdf_utils.py
      via: "from rosetta.core.rdf_utils import load_graph, save_graph, bind_namespaces, _PROV, ROSE_NS"
```

---

## Context

Phase 7 implements `rosetta-provenance`, which stamps PROV-O metadata onto mapping artifacts produced by earlier phases. The stub `rosetta/cli/provenance.py` already exists with a `cli` entrypoint that exits with code 1. This plan replaces it with a working Click group with `stamp` and `query` subcommands.

**Key constraints from prior phases:**
- `_PROV` namespace (`http://www.w3.org/ns/prov#`) is already declared in `rdf_utils.py`
- `ROSE_NS` (`http://rosetta.interop/ns/`) is the namespace for `rose:version` and activity/agent URIs
- All user-facing JSON outputs use Pydantic v2 models (Phase 5 convention)
- `rosetta-provenance` entrypoint is already registered in `pyproject.toml`
- No new pip dependencies needed — rdflib 6.3+ covers all PROV-O needs

**Autonomous decisions** (see `.decisions-pending.md`):
- D7-01: In-place augmentation (no sidecar)
- D7-02: UUID activity URI + default agent `rose:agent/rosetta-cli`
- D7-03: `rose:version` integer literal, incremented per stamp
- D7-04: Subcommand group (`stamp` / `query`); `--format json` for machine output
- D7-05: Minimal PROV-O triple set (Activity + Entity + Agent)

---

## Tasks

### Task 1 — Add ProvenanceRecord model to rosetta/core/models.py

**File:** `rosetta/core/models.py`

Append after the `EmbeddingReport` class:

```python
# --- Provenance ---

class ProvenanceRecord(BaseModel):
    activity_uri: str
    agent_uri: str
    label: str | None
    started_at: str   # ISO 8601 datetime string
    ended_at: str     # ISO 8601 datetime string
    version: int
```

No other changes to models.py.

**Done when:** `from rosetta.core.models import ProvenanceRecord` imports without error and `ProvenanceRecord(activity_uri="x", agent_uri="y", label=None, started_at="2026-01-01T00:00:00Z", ended_at="2026-01-01T00:00:00Z", version=1)` round-trips through `.model_dump(mode="json")`.

---

### Task 2 — Implement rosetta/core/provenance.py

**File:** `rosetta/core/provenance.py` (new file)

Implement two public functions with full type annotations:

#### `stamp_artifact`

```python
def stamp_artifact(
    g: Graph,
    artifact_uri: str,
    agent_uri: str = "http://rosetta.interop/ns/agent/rosetta-cli",
    label: str | None = None,
    now: datetime | None = None,
) -> int:
```

Logic:
1. Determine `now` (use `datetime.now(timezone.utc)` if not provided — injecting `now` enables deterministic tests).
2. SPARQL `SELECT (MAX(?v) AS ?max_v) WHERE { <artifact_uri> rose:version ?v }` — coerce result to int (default 0 if no rows or NULL).
3. `new_version = max_v + 1`.
4. Remove any existing `<artifact_uri> rose:version ?any` triple via `g.remove((URIRef(artifact_uri), ROSE_NS.version, None))`.
5. Add new version triple: `g.add((URIRef(artifact_uri), ROSE_NS.version, Literal(new_version, datatype=XSD.integer)))`.
6. Generate activity URI: `ROSE_NS[f"activity/{uuid4()}"]`.
7. Add PROV-O triples (see D7-05 in `.decisions-pending.md`):
   - `activity a prov:Activity`
   - `activity prov:startedAtTime Literal(now.isoformat(), datatype=XSD.dateTime)`
   - `activity prov:endedAtTime Literal(now.isoformat(), datatype=XSD.dateTime)`
   - `activity prov:wasAssociatedWith URIRef(agent_uri)`
   - If label: `activity rdfs:label Literal(label)`
   - `URIRef(artifact_uri) a prov:Entity`
   - `URIRef(artifact_uri) prov:wasGeneratedBy activity`
   - `URIRef(agent_uri) a prov:Agent`
8. Bind namespaces: call `bind_namespaces(g)` and also `g.bind("xsd", XSD)` and `g.bind("rdfs", RDFS)`.
9. Return `new_version`.

#### `query_provenance`

```python
def query_provenance(
    g: Graph,
    artifact_uri: str,
) -> list[ProvenanceRecord]:
```

Logic:
1. SPARQL SELECT over the graph to fetch activity, agent, label, startedAtTime, endedAtTime.
2. For version: join on `<artifact_uri> rose:version ?v` (there is only one version triple — the current one). For historical per-activity versions, embed version into the activity URI is not feasible; instead, report the current `rose:version` on every record (acceptable for MVP).
3. Coerce None-guarded OPTIONAL vars (e.g., `?label`) to Python None using the rdflib SPARQL gotcha pattern: `str(row["label"]) if row["label"] is not None else None`.
4. Return list of `ProvenanceRecord` instances sorted by `started_at` ascending.

**SPARQL query skeleton:**
```sparql
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX rose: <http://rosetta.interop/ns/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?activity ?agent ?label ?started ?ended ?version WHERE {
    <{artifact_uri}> prov:wasGeneratedBy ?activity ;
                     rose:version ?version .
    ?activity prov:startedAtTime ?started ;
              prov:endedAtTime   ?ended ;
              prov:wasAssociatedWith ?agent .
    OPTIONAL { ?activity rdfs:label ?label }
}
ORDER BY ASC(?started)
```

**Important:** Use `initBindings` for the artifact URI to avoid SPARQL injection. Pass `{"artifact": URIRef(artifact_uri)}` and reference `?artifact` in the query rather than f-string interpolating the URI.

**Imports needed:**
```python
from __future__ import annotations
import uuid
from datetime import datetime, timezone
from rdflib import Graph, Literal, Namespace, URIRef
from rdflib.namespace import PROV, RDFS, XSD
from rosetta.core.rdf_utils import bind_namespaces, ROSE_NS
from rosetta.core.models import ProvenanceRecord
```

Note: rdflib 6.x ships `rdflib.namespace.PROV` — use it instead of the private `_PROV` from rdf_utils.

**Done when:** Unit tests in Task 4 pass; `basedpyright` reports zero errors on this file.

---

### Task 3 — Implement rosetta/cli/provenance.py (full CLI)

**File:** `rosetta/cli/provenance.py` (replace stub)

Structure: Click group `cli` with two subcommands `stamp` and `query`.

```python
@click.group()
@click.option("--config", "-c", default=None, help="Path to rosetta.toml.")
@click.pass_context
def cli(ctx: click.Context, config: str | None) -> None:
    """Record and query provenance metadata for mapping decisions."""
    ctx.ensure_object(dict)
    ctx.obj["config"] = config
```

#### `stamp` subcommand

```
rosetta-provenance stamp <INPUT> [--output PATH] [--agent URI] [--label TEXT] [--format text|json]
```

Options:
- `INPUT` — positional argument: path to input Turtle artifact (required)
- `--output / -o` — output path; defaults to stdout if omitted
- `--agent` — agent URI string; default `"http://rosetta.interop/ns/agent/rosetta-cli"`
- `--label / -l` — human-readable label for this stamp event
- `--format` — `text` (default) or `json`

Logic:
1. Load graph from INPUT path via `load_graph(Path(input_path))`.
2. Determine artifact URI: `ROSE_NS[Path(input_path).stem]` as the canonical artifact URI. (Simple heuristic: ties provenance to the file stem. Documented in help text.)
3. Call `stamp_artifact(g, artifact_uri, agent_uri, label)` → returns `new_version`.
4. Save augmented graph via `save_graph(g, output_path_or_stdout)`.
5. If `--format json`: also print a JSON object to stderr (or a separate `--provenance-out` flag) — **decision: print stamp summary to stderr as JSON** so stdout remains clean Turtle. Use `ProvenanceRecord`-compatible dict for the summary.
6. Print confirmation to stderr: `Stamped <artifact_uri> version <N>`.
7. Exit 0 on success, 1 on any exception (with error message to stderr).

#### `query` subcommand

```
rosetta-provenance query <INPUT> [--format text|json]
```

Options:
- `INPUT` — positional argument: path to stamped Turtle artifact
- `--format` — `text` (default) or `json`

Logic:
1. Load graph from INPUT.
2. Determine artifact URI (same `ROSE_NS[stem]` heuristic).
3. Call `query_provenance(g, artifact_uri)` → list of `ProvenanceRecord`.
4. If `--format text`: print one line per record: `v{version}  {started_at}  {agent_uri}  {label or "(no label)"}`.
5. If `--format json`: `print(json.dumps([r.model_dump(mode="json") for r in records], indent=2))`.
6. Exit 0; if no records found, print `"No provenance records found."` to stderr and exit 0.

**Done when:** `uv run rosetta-provenance stamp tests/fixtures/nor_schema.ttl --label "test"` produces valid Turtle on stdout with PROV-O triples; `uv run rosetta-provenance query` prints stamp events.

---

### Task 4 — Write tests in rosetta/tests/test_provenance.py

**File:** `rosetta/tests/test_provenance.py` (new file)

Write pytest tests covering:

```python
# Fixtures
@pytest.fixture
def empty_graph() -> Graph:
    """A graph with a single subject triple to stamp."""

@pytest.fixture  
def stamped_graph(empty_graph: Graph) -> tuple[Graph, int]:
    """Graph stamped once; returns (graph, version)."""
```

**Test cases:**

1. `test_stamp_returns_version_1_on_first_call` — fresh graph, stamp → returns 1.
2. `test_stamp_increments_version` — stamp twice → first returns 1, second returns 2.
3. `test_stamp_adds_prov_entity_triple` — after stamp, `(artifact_uri, RDF.type, PROV.Entity)` is in graph.
4. `test_stamp_adds_activity_triple` — at least one `(_, RDF.type, PROV.Activity)` triple in graph.
5. `test_stamp_adds_agent_triple` — `(agent_uri, RDF.type, PROV.Agent)` in graph.
6. `test_stamp_datetime_injected` — pass `now=datetime(2026,1,1,tzinfo=timezone.utc)`, assert `prov:startedAtTime` literal equals expected ISO string.
7. `test_stamp_label_optional` — stamp without label, no `rdfs:label` triple on activity.
8. `test_stamp_label_present` — stamp with label="foo", `rdfs:label` triple on activity equals "foo".
9. `test_query_empty_graph_returns_empty_list` — no stamps → `query_provenance` returns `[]`.
10. `test_query_returns_one_record_after_stamp` — stamp once → query returns list of length 1 with correct fields.
11. `test_query_returns_two_records_after_two_stamps` — stamp twice → query returns list of length 2.
12. `test_provenance_record_model_roundtrip` — `ProvenanceRecord` serializes/deserializes via `model_dump(mode="json")`.

**CLI integration tests** (use `click.testing.CliRunner`):
13. `test_cli_stamp_writes_valid_turtle` — CliRunner invokes `stamp` on a temp TTL file, output is parseable RDF.
14. `test_cli_stamp_exits_zero` — exit code 0.
15. `test_cli_query_after_stamp` — stamp then query, text output contains "v1".
16. `test_cli_query_json_format` — `--format json` output is valid JSON array.

Annotation note: all fixture return types and test signatures must be annotated (basic basedpyright mode).

**Done when:** `uv run pytest rosetta/tests/test_provenance.py -v` shows all 16 tests passing.

---

## Quality Gates

Before marking phase complete, run all four mandatory checks:

```bash
uv run ruff format .
uv run ruff check .
uv run basedpyright
uv run pytest -m "not slow"
```

All must exit 0 with zero errors/warnings.

---

## Execution Order

Task 1 → Task 2 → Task 3 → Task 4 (sequential; each task builds on the previous).

Task 1 must complete before Task 2 (ProvenanceRecord imported by core/provenance.py).
Task 2 must complete before Task 3 (CLI imports core functions).
Task 4 can be written alongside Task 3 but requires Tasks 1+2 to pass.
